import {
    Pen,
    Hand,
    MousePointer,
    Eraser,
    Circle,
    Minus,
    Undo,
    Redo,
    ZoomIn,
    ZoomOut,
    createIcons,
} from 'lucide'

createIcons({
    icons: {
        Pen,
        Hand,
        MousePointer,
        Eraser,
        Circle,
        Minus,
        Undo,
        Redo,
        ZoomIn,
        ZoomOut,
    },
})
